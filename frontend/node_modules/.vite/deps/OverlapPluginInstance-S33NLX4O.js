import "./chunk-QPPA7URK.js";
import {
  getDistance
} from "./chunk-NDKBCCV3.js";
import {
  __privateAdd,
  __privateGet,
  __privateSet
} from "./chunk-SNAQBZPT.js";

// node_modules/@tsparticles/interaction-particles-collisions/browser/OverlapPluginInstance.js
var minRetries = 0;
var _container, _hasOverlaps;
var OverlapPluginInstance = class {
  constructor(container) {
    __privateAdd(this, _container);
    __privateAdd(this, _hasOverlaps, (particle, pos, tryCount) => {
      const collisionsOptions = particle.options.collisions;
      if (!(collisionsOptions == null ? void 0 : collisionsOptions.enable)) {
        return false;
      }
      const overlapOptions = collisionsOptions.overlap;
      if (overlapOptions.enable) {
        return false;
      }
      const retries = overlapOptions.retries;
      if (retries >= minRetries && tryCount > retries) {
        throw new Error(`Particle is overlapping and can't be placed`);
      }
      return !!__privateGet(this, _container).particles.find((p) => getDistance(pos, p.position) < particle.getRadius() + p.getRadius());
    });
    __privateSet(this, _container, container);
  }
  checkParticlePosition(particle, position, tryCount) {
    return !__privateGet(this, _hasOverlaps).call(this, particle, position, tryCount);
  }
};
_container = new WeakMap();
_hasOverlaps = new WeakMap();
export {
  OverlapPluginInstance
};
//# sourceMappingURL=OverlapPluginInstance-S33NLX4O.js.map
